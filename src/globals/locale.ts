import enCA from "@/locales/en-CA.json";
import ptBR from "@/locales/pt-BR.json";

const locale: string = "en-CA";
const fallback: string = "en-CA";
const messages: any = { "en-CA": enCA, "pt-BR": ptBR };

export default { locale, fallback, messages };
